packages:
  - "packages/*"
  - "templates/*"

# Registry semver ranges that match an in-workspace package link to the
# workspace copy (mirrors the private monorepo setting).
linkWorkspacePackages: true
